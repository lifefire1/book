spring:
  application:
    name: pdf
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true

  datasource:
    username: test
    password: test
    url: jdbc:postgresql://localhost:5432/dictionary

